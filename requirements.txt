python-nmap     # Optional: Allows scanning your own local network using Nmap to detect devices, open ports, and basic OS info.
pandas          # Required: Used to create, manage, and export the classification table (OS, device type, role, criticality) as CSV or JSON.
tabulate        # Optional but recommended: Formats the table neatly in the console for easy viewing of classification results.
